# --- Core backend ---------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic>=2.10.5
pydantic-settings>=2.7.1
python-dotenv==1.0.1

# --- AI orchestration -------------------------------------------------------
langchain>=1.3.0
langchain-core>=1.4.0
langgraph>=1.2.0
langmem==0.0.30

# LLM providers (all installed so LLM_PROVIDER can be switched without
# reinstalling anything; only the selected provider's API key is required
# at runtime)
langchain-openai>=1.3.0
langchain-groq>=1.1.0
langchain-google-genai>=4.2.0

# --- Retrieval / RAG --------------------------------------------------------
chromadb>=1.3.5
langchain-chroma>=1.1.0
langchain-text-splitters>=1.1.0

# --- Data & UI ---------------------------------------------------------------
streamlit==1.41.1

# --- Utilities ---------------------------------------------------------------
requests==2.32.3
